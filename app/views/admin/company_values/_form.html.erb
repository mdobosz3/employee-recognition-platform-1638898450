<%= form_with(model: [ :admin, company_value ]) do |form| %>
  <% if company_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_value.errors.count, "error") %> prohibited this admin_company_value from being saved:</h2>

      <ul>
        <% company_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :title %><br>
    <%= form.text_field :title, class:"form-control" %>
  </div>
<br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
