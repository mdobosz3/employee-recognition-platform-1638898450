<%= form_with(model: admin_kudo) do |form| %>
  <% if admin_kudo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_kudo.errors.count, "error") %> prohibited this admin_kudo from being saved:</h2>

      <ul>
        <% admin_kudo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
